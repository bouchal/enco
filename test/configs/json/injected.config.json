{
    "production": {
        "host": #{host},
        "port": #{server.port},
        "mysql": #{databases.mysql.host},
        "escaped": #{escaped}
    },

    "development": {
        "port": 8080
    },

    "localhost": {
        "port": 3000
    }
}